import React from "react";
import { motion } from "framer-motion";
import { useNavigate } from "react-router-dom";
import { TransitionDiv } from '../Motion/MotionElements'

const Home = () => {
  return (
    <TransitionDiv className="home">

    </TransitionDiv>
  );
};

export default Home;